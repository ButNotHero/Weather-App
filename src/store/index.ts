import { createStore } from 'vuex';
import { currentWeather } from '@/store/currentWeather.module';

export default createStore({
  state: {},
  mutations: {},
  actions: {},
  modules: {
    currentWeather,
  },
});
